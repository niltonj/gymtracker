services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: gymtracker
      POSTGRES_USER: gym
      POSTGRES_PASSWORD: gym
    ports: ["5432:5432"]
    volumes:
      - ./seed.sql:/docker-entrypoint-initdb.d/seed.sql:ro
